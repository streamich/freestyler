import globalCss from './global';

const css = {
    '*': {
        pad: 0,
        mar: 0,
        bd: 0,
    },
};

const CssResetMinimalistic2 = globalCss(css);

export default CssResetMinimalistic2;
